public class WritePoetry {

    /*
    * @PARAMS@
    * file - document to be read
    * startWord - is the first word of poem to be generated
    * length - how many words (incld punctuation) to generate
    *   new line after punctuation
    *   end with a period if no other punctuation
    *   end punctuation counts as a word
    * printHashtable - if true: print hashtable to console; else: don't print
    */
    public String writePoem(String file, String startWord, int length, boolean printHashtable)
    {



        if (printHashtable)
        {
            // print hashtable out
            ;
        }
        return "";
    }
}
